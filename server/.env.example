# Spotify Backend Configuration
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env in the same directory:
#    cp .env.example .env
#
# 2. Open server/.env and fill in your real Spotify credentials
#    DO NOT commit .env to Git - it contains your secret keys
#
# 3. Get your credentials from: https://developer.spotify.com/dashboard
#
# IMPORTANT:
# - Never commit the .env file
# - The .env file is gitignored and should stay that way
# - Only paste your real values in your local server/.env file
# - The SPOTIFY_REDIRECT_URI must match EXACTLY what you add in Spotify Dashboard → Redirect URIs

# Spotify Client ID (from Spotify Developer Dashboard)
SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Spotify Client Secret (from Spotify Developer Dashboard)
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# Redirect URI - must match EXACTLY what you configured in Spotify dashboard
# IMPORTANT: Add this exact URI in Spotify Dashboard → Redirect URIs:
# http://127.0.0.1:3001/auth/callback
SPOTIFY_REDIRECT_URI=http://127.0.0.1:3001/auth/callback

# Frontend URL for CORS and post-auth redirect
FRONTEND_URL=http://localhost:5173

# Server port (optional, defaults to 3001)
PORT=3001
