import ContactForm from "./ContactForm.jsx";

/**
 * Contact - Page component that renders the ContactForm
 * Separated for consistency with other page components
 */
export default function Contact() {
  return <ContactForm />;
}